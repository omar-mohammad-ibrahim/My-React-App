/** @type {import('tailwindcss').Config} */
export default {
  // 1. تفعيل الوضع الليلي: يتيح لك استخدام كلاس dark:bg-surface-dark عند إضافة كلاس dark لوسم <html>
  darkMode: "class",

  // 2. الملفات المراقبة: الأماكن التي يبحث فيها Tailwind عن الكلاسات لحذف الكود غير المستخدم
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],

  theme: {
    // 3. الحاوية (Container): إعدادات الحاوية العامة للصفحة
    container: {
      center: true, // يوسّط المحتوى تلقائياً في منتصف الشاشة (كأنك كتبت mx-auto)
      padding: {
        DEFAULT: "1rem", // مسافة داخلية 16px على الشاشات الصغيرة
        sm: "2rem", // مسافة داخلية 32px على الشاشات المتوسطة
        lg: "4rem", // مسافة داخلية 64px على الشاشات الكبيرة
      },
    },

    extend: {
      colors: {
        // ==========================================
        // 4. اللون الأساسي (Primary) - هوية التطبيق والأزرار الأهم
        // ==========================================
        primary: {
          // --- التدرجات (Shades): تستخدم للخلفيات الخفيفة والحدود وحالات التفاعل ---
          50: "#fff8f0", // خلفيات التنبيهات الفاتحة والـ Badges (مثال: bg-primary-50)
          100: "#ffebd4", // خلفية خفيفة عند الـ Hover على عنصر بالقائمة (مثال: hover:bg-primary-100)
          200: "#ffd3a8", // حدود الكروت والفواصل (مثال: border-primary-200)
          300: "#ffb371", // حدود حقول الإدخال عند الـ Focus (مثال: focus:border-primary-300)
          400: "#ff8933", // أيقونات أو نصوص متوسطة الوضوح (مثال: text-primary-400)
          500: "#ff6600", // اللون الأساسي المعتمد للتطبيق
          600: "#e65200", // لون أغمق لحالة الـ Hover للزر (مثال: hover:bg-primary-600)
          700: "#b83b00", // لون النصوص الغامقة فوق خلفية فاتحة (مثال: text-primary-700)
          800: "#942f00", // لون عند الضغط المستمر على الزر (مثال: active:bg-primary-800)
          900: "#7a2700", // أغمق درجة للعناوين الرئيسية (مثال: text-primary-900)

          // --- الأسماء المباشرة (Direct Aliases) ---
          DEFAULT: "#ff6600", // اللون الافتراضي عند كتابة (bg-primary أو text-primary أو border-primary)
          hover: "#e65200", // لون الزر عند مرور الماوس (مثال: hover:bg-primary-hover)
          foreground: "#ffffff", // لون الخط الأنسب فوق الخلفية البرتقالية (مثال: text-primary-foreground)
        },

        // ==========================================
        // 5. اللون الثانوي (Secondary) - للإجراءات البديلة والعناوين المميزة
        // ==========================================
        secondary: {
          50: "#fff3eb", // خلفية ثانوية خفيفة جداً (مثال: bg-secondary-50)
          100: "#ffe3d1", // حدود خفيفة للعناصر الثانوية (مثال: border-secondary-100)
          500: "#d04a0a", // اللون الثانوي المعتمد
          600: "#b33c06", // درجة أغمق للـ Hover (مثال: hover:bg-secondary-600)
          700: "#8c2d04", // نصوص ثانوية بارزة (مثال: text-secondary-700)
          DEFAULT: "#d04a0a", // يُستخدم مباشرة عبر (bg-secondary أو text-secondary)
          hover: "#b33c06", // لون التمرير المخصص (مثال: hover:bg-secondary-hover)
          foreground: "#ffffff", // لون النص فوق الخلفية الثانوية (مثال: text-secondary-foreground)
        },

        // ==========================================
        // 6. ألوان الأسطح والبطاقات (Surfaces) - ممتازة للوضع الليلي والنهاري
        // ==========================================
        surface: {
          light: "#ffffff", // خلفية البطاقة في الوضع العادي (مثال: bg-surface-light)
          dark: "#0f172a", // خلفية البطاقة في الوضع الليلي (مثال: dark:bg-surface-dark)
          card: "#f8fafc", // خلفية الكروت الداخلية أو الأقسام الرمادية (مثال: bg-surface-card)
        },

        // ==========================================
        // 7. النصوص الخافتة وغير البارزة (Muted)
        // ==========================================
        muted: {
          DEFAULT: "#64748b", // للنصوص الفرعية مثل التواريخ والتفاصيل (مثال: text-muted)
          foreground: "#94a3b8", // للنصوص التوضيحية داخل الـ Inputs (مثال: placeholder:text-muted-foreground)
        },

        // ==========================================
        // 8. حالات النظام (Danger & Success)
        // ==========================================
        danger: {
          DEFAULT: "#ef4444", // لعمليات الحذف والتنبيهات الحمراء (مثال: bg-danger أو text-danger)
          hover: "#dc2626", // لون زر الحذف عند التمرير (مثال: hover:bg-danger-hover)
        },
        success: "#10b981", // لعمليات النجاح وحفظ البيانات ورسائل التأكيد (مثال: bg-success أو text-success)
      },

      // ==========================================
      // 9. الخطوط المخصصة (Font Family)
      // ==========================================
      fontFamily: {
        // يطبق خط Cairo ثم Inter تلقائياً عند كتابة كلاس (font-sans)
        sans: ["Cairo", "Inter", "sans-serif"],
      },

      // ==========================================
      // 10. الحواف الدائرية (Border Radius)
      // ==========================================
      borderRadius: {
        // يمنح العناصر انحناء حواف بقيمة 10px الموحدة للهوية (مثال: rounded-brand)
        brand: "10px",
      },

      // ==========================================
      // 11. الظلال المخصصة (Box Shadow)
      // ==========================================
      boxShadow: {
        // توهج برتقالي خفيف حول الكروت والأزرار الأساسية (مثال: shadow-primary-glow)
        "primary-glow": "0 4px 20px -2px rgba(255, 102, 0, 0.35)",
      },

      // ==========================================
      // 12. قسم التدرجات والصور الخلفية (Background Images)
      // ==========================================
      backgroundImage: {
        // تدرج من اليسار لليمين: ناري -> برتقالي -> أصفر
        // الاستخدام: className="bg-brand-gradient"
        "brand-gradient":
          "linear-gradient(to right, #ff3000, #ff6a00, #ffaa00)",

        // تدرج معاكس من اليسار لليمين: أصفر -> برتقالي -> ناري
        // الاستخدام: className="bg-brand-gradient-reverse"
        "brand-gradient-reverse":
          "linear-gradient(to right, #ffaa00, #ff6a00, #ff3000)",
      },
    },
  },

  // 12. مصفوفة الإضافات (Plugins): لإضافة مكتبات Tailwind الجاهزة مستقبلاً
  plugins: [],
};
